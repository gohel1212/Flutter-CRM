# Prisma Database Connection URL
# Replace username, password, host, port, and database name with your actual MySQL credentials
DATABASE_URL="mysql://root:@127.0.0.1:3306/simple_crm"

# Secret key for JWT signing (use a long, secure random string in production)
JWT_SECRET="crm_super_secret_key_12345"
